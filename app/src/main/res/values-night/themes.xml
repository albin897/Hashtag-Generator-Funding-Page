<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.HashtagGenerator" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
        <item name="android:windowLightStatusBar" tools:targetApi="m">false</item>
        <item name="android:windowBackground">@color/app_background_color</item>

<!--        <item name="android:navigationBarColor" tools:targetApi="lollipop">?attr/colorSurface</item>-->
    </style>

    <style name="Theme.HashtagGenerator" parent="Base.Theme.HashtagGenerator">
    </style>
</resources>